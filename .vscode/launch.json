{
    "version": "0.2.0",
    "configurations": [
    
        {
            "name": "Launch distributed training (master)",
            "type": "debugpy",
            "request": "launch",
            //"program": "${workspaceFolder}/script/downstream.py",
            "module": "torch.distributed.launch",
            "console": "integratedTerminal",
            "justMyCode": false,
            "args": [
                //"-m", "torch.distributed.launch",
                "--nproc_per_node=1",
                "${workspaceFolder}/script/downstream.py",
                "-c", "config.yaml",
                "--gpus", "[0]"
            ],
  
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
                
            }
        }
    ]
}
 


//nohup python -m torch.distributed.launch --nproc_per_node=2 script/downstream.py -c config.yaml --gpus [0,1] &> /home/xiaotong/scratch/protein_output/MultipleBinaryClassification/EnzymeCommission/DGMGearnet/log/newLog.out&